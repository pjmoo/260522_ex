# TIL | 2026-05-22

## 오늘 한 일

오늘은 JavaScript 기초 문법과 동작 원리를 파일별 실습으로 정리했다.  
반복문, 함수, 매개변수, 호이스팅, 스코프, 클로저, 객체, 배열까지 이어지는 흐름을 한 번에 복습하면서, 단순 문법 암기보다 실행 순서와 변수의 생명주기를 함께 이해하는 데 집중했다.

## 실습한 내용

### `01_for.js`
- `for` 반복문의 기본 구조를 익혔다.
- 초기값, 조건식, 증감식이 어떤 순서로 동작하는지 확인했다.
- 반복문을 사용할 때 무한 루프가 발생하지 않도록 조건을 명확하게 잡는 것이 중요하다는 점을 정리했다.

### `02_for+in+of.js`
- `for...in`과 `for...of`의 차이를 비교했다.
- `for...in`은 객체의 key를 순회할 때 유용하고, `for...of`는 iterable의 값을 순회할 때 적합하다는 점을 학습했다.
- 배열에는 보통 `for...of`가 더 자연스럽고 안전하다는 흐름을 이해했다.

### `03_function.js`
- 함수 선언과 함수 호출의 기본 흐름을 복습했다.
- 같은 기능도 함수로 분리하면 재사용성과 가독성이 좋아진다는 점을 확인했다.

### `04_parameter.js`
- 매개변수와 인자의 관계를 정리했다.
- 함수에 값을 전달해서 동작을 바꾸는 방식이 코드 재사용에 어떻게 도움이 되는지 확인했다.
- 기본값 처리나 입력값 검증이 필요할 수 있다는 점도 함께 떠올렸다.

### `05_.function2.js`
- 함수를 한 단계 더 확장해서 다루는 방식으로 실습했다.
- 단순 호출뿐 아니라 반환값을 활용하는 흐름, 또는 함수 내부 로직을 더 세분화하는 구조를 이해하는 데 도움이 됐다.

### `06_da.js`
- 데이터 다루기 관점에서 JavaScript의 값과 구조를 복습했다.
- 변수에 들어가는 값의 형태에 따라 처리 방식이 달라질 수 있다는 점을 정리했다.
- 원시값과 참조값의 차이를 이해하는 데 필요한 기초 감각을 잡았다.

### `07_hoisting.js`
- 호이스팅 개념을 확인했다.
- 선언이 실제 코드 상단으로 끌어올려지는 것처럼 보이는 이유를 이해했다.
- `var`, 함수 선언문, `let`, `const`의 차이를 함께 생각해보며 실행 전에 접근 가능한 범위와 불가능한 범위를 구분했다.

### `08_scope.js`
- 스코프의 범위를 정리했다.
- 전역 스코프와 지역 스코프의 차이, 그리고 블록 스코프의 개념을 다시 확인했다.
- 변수 이름 충돌을 줄이고 코드의 예측 가능성을 높이려면 스코프를 의식해서 작성해야 한다는 점을 배웠다.

### `09_closure.js`
- 클로저의 기본 동작을 이해했다.
- 함수가 실행된 이후에도 외부 변수에 접근할 수 있는 이유를 실습을 통해 정리했다.
- 상태를 은닉하거나 값을 기억하는 패턴에 클로저가 활용될 수 있다는 점이 인상적이었다.

### `10_object.js`
- 객체를 이용해 관련 데이터를 묶어서 표현하는 방법을 익혔다.
- 프로퍼티 접근 방식, 수정 방식, 필요한 정보만 추려 쓰는 방식 등을 정리했다.
- 객체는 실제 서비스 코드에서 데이터를 구조화할 때 매우 자주 쓰인다는 점을 다시 확인했다.

### `11_array.js`
- 배열의 기본 사용법을 정리했다.
- 값 추가, 삭제, 순회, 특정 요소 조회 같은 기본 작업을 실습했다.
- 배열 메서드를 활용하면 반복문을 직접 많이 쓰지 않아도 다양한 처리가 가능하다는 점을 복습했다.

## 오늘의 정리

- 반복문은 단순한 순회 도구가 아니라, 데이터 처리의 기본 패턴이다.
- 함수는 코드를 재사용 가능하게 만들고, 매개변수는 함수의 유연성을 높인다.
- 호이스팅과 스코프를 이해해야 왜 어떤 변수는 먼저 사용되고 어떤 변수는 에러가 나는지 설명할 수 있다.
- 클로저는 JavaScript의 핵심 개념 중 하나로, 상태를 기억하는 함수를 만들 때 유용하다.
- 객체와 배열은 실제 데이터 구조를 다루는 가장 기본적인 도구라서, 문법뿐 아니라 활용 패턴까지 익히는 게 중요하다.

## 느낀 점

오늘은 개별 문법을 따로 보는 것보다, 서로 어떻게 연결되는지를 같이 보는 게 더 중요하다는 걸 느꼈다.  
특히 함수, 스코프, 호이스팅, 클로저는 따로 보면 어렵지만 함께 보면 JavaScript 실행 방식이 훨씬 선명해진다.

## 내일 이어서 할 것

- 배열 메서드와 객체 메서드를 더 많이 사용해 보기
- 함수 선언식, 함수 표현식, 화살표 함수 차이 다시 정리하기
- 스코프와 클로저 예제를 직접 다시 작성해 보기

